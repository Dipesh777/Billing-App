import React from 'react'
import AuthContainer from './Authentication/AuthContainer'

const Routers = (props) => {
    return (
        <main>
            <AuthContainer />
        </main>
    )
}

export default Routers